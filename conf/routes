# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# TodoController
GET         /todos            web.TodoController.findAll
POST        /todos            web.TodoController.create
GET         /todos/:id        web.TodoController.findOne(id: Long)
DELETE      /todos/:id        web.TodoController.remove(id: Long)